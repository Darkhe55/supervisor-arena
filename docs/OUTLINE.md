# Supervisor Arena — 项目大纲

> 给导师打分的"众包 + 动态加权"排名系统。
> 核心理念:**没有绝对好坏,所有评分都是相对的、综合的**。

---

## 1. 项目定位

### 1.1 一句话
> 任何账号(默认匿名)都可以发布新导师条目 + 多维度评分;系统通过**综合评分** + **学科自适应的权重投票** + **无关化名** + **k-匿名**给出推荐程度。

### 1.2 核心价值
- **用户分层**:消费端是**考生/择校者**(主)+ **转专业学生**(次);数据源是**在读学生**;**老师完全禁止**参与。
- **无权威化**:不依赖少数"权威评审",靠学生投票 + 数学加权。
- **去绝对化**:不输出"好/坏"标签,只输出"综合推荐度" + "维度画像"。判断在用户手里。
- **可演化**:评分有时间序列,同一人再次评分 = 覆盖当前值。
- **会员制**:基础信息免费,详细数据(均值变动曲线/共识度/置信区间)会员制。
- **国际化**:中英双语。
- **无关化名**(法律核心):化名由平台生成,**必须**与真实人名无关。平台主动声明"不对应任何真人",进一步降低法律风险。

### 1.3 不做什么(反目标)
- ❌ 不做"权威认证 / 官方背书"标签。
- ❌ 不做单一总分的"及格线"。
- ❌ 不让**老师**直接参与(避免刷分) — 软移除。
- ❌ 不做收费排名 / 竞价排名。
- ❌ 不开放纯讨论区(避免过激讨论)。
- ❌ 不做"实名推荐"功能。
- ❌ **法律合规优先于功能**:任何可能被解读为"具体指向真人"的特性都需先做法律评估。
- ❌ 不在公开数据中暴露科研内容细节(未发表数据/技术细节/合作项目等)。
- ❌ **不向用户提供**任何"原始名 ↔ 化名"映射查询通道。
- ❌ **不在公开页面**显示真名、真实机构、方向、资历、描述、头像等任何直接/间接识别信息。
- ❌ **不验证**用户提交的名称是否对应真实的人。
- ❌ **不赋予审核员查询权**(只审隐私/恶意,不查映射表)。
- ❌ **不提供"侵权投诉"功能**(无指向真人的实名信息)。
- ❌ **不做"导师自证"功能**(化名无关真人)。
- ❌ **不做"误认救济"功能**(化名与真人无对应)。
- ❌ **不做导出功能**。

### 1.4 用户分层
```
┌──────────────────────────────────────────────────────────┐
│ 数据源(必须是在读学生)                                   │
│   ↓ 提交评分(匿名,受隐私制度保护)                        │
├──────────────────────────────────────────────────────────┤
│ 系统聚合层(学科自适应权重,反老师刷分)                     │
│   ↓ 输出综合分 + 雷达图 + (会员)共识度/置信区间/均值曲线  │
├──────────────────────────────────────────────────────────┤
│ 消费端(基础)                                             │
│   - 考生(主) — 选导师,看综合分                          │
│   - 择校者(主) — 选学校/学科,横向对比                    │
│   - 转专业学生(次) — 看学科适配                          │
└──────────────────────────────────────────────────────────┘
       会员解锁:均值变动曲线 / 共识度 / 置信区间
       任何用户可用:对比功能
```

---

## 2. 核心概念

| 概念 | 含义 | 关键属性 |
|------|------|----------|
| **账号 (Account)** | 系统内唯一身份 | 注册时声明学科+学校,内部 hash,对外不展示 |
| **学生身份 (Student Profile)** | 注册时声明的"在读"信息 | 学科 / 学校 / 年级 — 用于学科相关性加权 |
| **匿名发布 (Anonymous Mode)** | 评分默认状态 | 任何用户**无法查看具体评分来源**和总评价人数 |
| **导师条目 (Supervisor)** | 被评分的对象 | 公开字段:化名 + 学科 + 学院 + 评分数据 |
| **评分 (Rating)** | 对导师某一维度的打分 | **0–100 连续值**;**滑块 + 可选附加信息**结构 |
| **附加信息 (Additional Info)** | 滑块外的可选描述 | 每维度可选 + 总附加信息;**仅用于审核和聚合权重,不公开** |
| **覆盖 (Overwrite)** | 同一人再次评分 = 覆盖当前值 | 原始数据保留(用于轨迹) |
| **综合推荐分 (Composite Score)** | 学科自适应权重聚合后的输出 | 基础账号可见,0–100(均值不低于 0) |
| **雷达图 (Radar)** | 维度画像 | 基础账号可见 |
| **共识度 (Consensus)** | 评分者一致性 | 会员可见 |
| **置信区间 (Confidence)** | 统计置信度 | 会员可见 |
| **均值变动曲线 (Mean Trajectory)** | 综合统计均值的时间序列 | 会员可见,**不显示具体修改** |
| **化名 (Alias)** | 平台生成的无关代号 | 必须与人名白名单无关,跨学科+学院唯一 |
| **平台主动声明 (Declaration)** | 站内每个化名条目下的固定声明 | 强调"不对应真人" + "不提供侵权投诉" |

---

## 3. 评分维度(初版 6 项,可扩展)

每个维度 **0–100 分**;按学科可调整权重。

| 维度 | 含义 | 高分典型 | 低分典型 |
|------|------|----------|----------|
| **科研能力 (R)** | 论文/项目/产出的整体水平 | 顶会一作多、长期稳定产出 | 长期无产出、署名混乱 |
| **资源调度能力 (Res)** | 经费/实验室/合作网络/学生名额 | 项目充足、能拉来合作 | 经费紧张、无合作 |
| **学科适配性 (Fit)** | 导师方向与学生兴趣的契合度 | 方向清晰、组内传承好 | 频繁换方向、组员混杂 |
| **领域跟进度 (Curr)** | 对前沿/新方法的把握 | 用最新工具、跟最新会议 | 五年不更新教学/方法 |
| **行为正当性 (Ethic)** | 署名/引文/师生关系/利益冲突 | 公平署名、不抢学生一作 | 抢一作、PUA、压榨 |
| **新兴工具支持率 (Tool)** | 对学生使用 AI/新工具的态度 | 鼓励用 LLM/agent,给指导 | 禁止用、要求手写 |

> **可扩展**:按学科独立调整(CS 加重"工具",历史加重"跟进")。

---

## 4. 评分模型

### 4.1 评分表单结构
```
单条原始评分(滑块):
  R_user,supervisor,dim ∈ [0, 100]   (默认连续)
  + 时间戳 t
  + 学科标签 user.discipline
  + 覆盖关系 superseded_by (指向新评分)

附加信息(可选):
  - 每维度附加信息:dim_additional (该维度的可选描述)
  - 整体总附加信息:overall_additional (对整体的,可选)
  - 共同规则:NLP 自动识别证据词 → 等级 L1-L4
```

**UI 表现**:
```
科研能力 [====●=====] 75  [+ 添加附加信息]
资源调度 [======●===] 65  [+ 添加附加信息]
学科适配 [=====●====] 50  [+ 添加附加信息]
领域跟进 [======●===] 65  [+ 添加附加信息]
行为正当 [====●=====] 75  [+ 添加附加信息]
工具支持 [======●===] 65  [+ 添加附加信息]

总附加信息(对整体评价,可选):
[_________________________]
```

### 4.2 单维度聚合(对一位导师)
```
S_dim(supervisor, dim) = Σ(weight_i * R_i) / Σ(weight_i)
```
其中 `weight_i` 综合:
- **时间衰减**: `w_time = exp(-0.1 * (now - t_i))`,按学科微调
- **学科相关性**: 用户学科与导师学科的契合度
  - 完全匹配 = 1.0
  - 相关学科(CS ↔ Math) = 0.7
  - 跨学科(CS ↔ History) = 0.3
- **附加信息强度**: NLP 自动识别证据词(URL/数据/事件),等级 L1–L4(见 §6.2)
  - 滑块 + L4 附加信息(强证据) = 最高权重
  - 滑块 + 无附加信息 = 基础权重

### 4.3 综合推荐分(学科自适应权重)
```
Composite(supervisor, discipline) = Σ(α_dim(discipline) * normalize(S_dim))
                                  + β * diversity_bonus
                                  - γ * controversy_penalty
```
- `α_dim(discipline)`:**学科自适应权重**,由**用户共同投票**决定(见 §4.4)
- `diversity_bonus`:维度得分方差小 → 加分(全面型)
- `controversy_penalty`:评分争议大 → 减分
- **公式可在后续修改**:M+ 优化时调整系数,不破坏历史数据

### 4.4 学科权重投票机制
- 投票入口:每个学科详情页有"建议调整权重"按钮
- 投票门槛:同学科已提交 ≥ 3 条评分的用户可投
- 通过门槛:得票 ≥ 60% 同学科活跃用户
- 冷却期:同一维度权重 30 天内只能改一次

### 4.5 输出形式
- **基础账号**:
  - 综合推荐分: 0–100(均值不低于 0)
  - 6 维雷达图
  - **对比功能**(任何用户可用)
- **会员**:
  - 上述 + 共识度 (0–1)
  - 置信区间
  - **均值变动曲线**(时间序列)

> **关键设计**:**不输出"好/坏"标签**。

### 4.6 负分机制(受控开放)
- **默认不开放负分**:用户提交评分时,UI 滑块最低 0
- **触发条件**:同一账号对同一导师**多次降分** + 历时**超过半年**
- **开启方式**:触发后,系统对该账号、该导师**单独开放**负分入口
- **下降限制**:
  - 触发后**每过一个月**额度下降 **10**
  - 最高可低至 **-100**
- **公开端约束**:在公开的导师对应数据中,综合分均值**不低于 0**

### 4.7 评分人数限制(避免恶意集中刷分)
- **基础账号**:同一导师最多 **3 条** 评分(覆盖关系下只算 1 条当前值)
- **会员**:同一导师最多 **10 条** 评分
- **超出处理**:
  - 学科相关 → 加权计入(系数 0.3–0.7)
  - 完全不相关 → 不计入,留作旁证

### 4.8 人工审核
- **触发范围**:所有**新评分** + 所有**修改评分**(含滑块值 + 附加信息)
- **状态**:提交后进入 `pending_review`,不计入综合分
- **审核员**:M1 内部 1–3 人;M2+ 高信誉志愿者
- **审核标准**(任一不通过即打回):
  - 滑块值合理性(非刷分、非极端值、非重复)
  - 附加信息(如有):敏感词 / 科研内容泄露 / 内容合理性
- **审核 SLA**:24h 工作日 / 72h 非工作日;超时升级
- **不通过处理**:站内通知提交者(原因 + 申诉入口) + 评分不入综合分(保留记录追溯) + 30 天 ≥ 3 次不通过 → 信誉扣分
- **通过处理**:评分进入综合分 + 通知提交者 + 感谢

### 4.9 敏感信息禁止清单
- **P0 严禁**(发现即打回):
  - 未发表论文/数据/实验细节
  - 实验室内部技术细节
  - 商业合作项目细节
  - 学生未公开的研究内容
  - 私人联系方式
  - 医疗/财务等敏感个人信息
- **P1 脱敏**(自动替换后才通过):真名/机构/实验室/项目代号
- **P2 警告**(提示提交者但允许):公开论文标题/演讲

---

## 5. 动态相对修正(暂定延后讨论)

> **当前状态**:用户未明确,默认按 OUTLINE §5 草案(守恒修正 + 防刷分)。
> 推迟到 M+ 决定是否实施。

### 5.1 触发条件
- 任意时刻可提交证据,系统根据证据强度、共识度决定是否采纳

### 5.2 守恒修正公式
- `S_A += ε, S_B -= ε` (守恒,无绝对标尺)
- `ε = f(证据强度, 共识度)`,范围 0.05–0.5
- 30 天冷却,新证据可反驳旧修正

### 5.3 防刷分
- 证据必须含 URL 或具体事件
- L3 证据以下自动,L3 以上进仲裁
- 被影响方 3 人以上反对则进仲裁

---

## 6. 附加信息 + 均值变动曲线

> **v0.9 关键设计**:
> - **没有"修改时间线" / "修改理由" 展示**(严格意义上)
> - 替代:**均值变动曲线**(综合统计均值的时间序列,会员可见)
> - 附加信息**仅用于**审核和聚合权重,**不展示**给其他用户(包括会员)

### 6.1 评分覆盖的结构
```json
{
  "user": "anon#abc123",
  "supervisor": "...",
  "dim": "research",
  "value": 75.0,            // 当前值
  "original_value": 60.0,    // 上一次的值(覆盖前)
  "dim_additional": "...",   // 该维度的附加信息(可选)
  "overall_additional": "看到该组学生延期普遍...",  // 整体附加信息(可选)
  "evidence": ["https://..."],
  "discipline": "CS",
  "timestamp": "...",
  "superseded_by": null      // 再次覆盖时指向新记录
}
```

### 6.2 附加信息分级(NLP 自动识别)
- **L1 — 简单感受**: 1–2 句,无证据 → weight 0.5
- **L2 — 具体观察**: 描述事件/数据 → weight 1.0
- **L3 — 引用支撑**: 含 URL → weight 1.3
- **L4 — 多人背书**: 多名匿名用户独立提交相似证据 → weight 1.5

> **NLP 自动识别**:扫描"具体年份/数字/URL/事件名"等关键词,自动归级。
> 附加信息内容**仅用于审核和聚合权重**,**不展示**给其他用户。

### 6.3 公开数据可见性
| 内容 | 基础账号 | 会员 |
|------|----------|------|
| 当前综合分 | ✅ | ✅ |
| 6 维雷达图 | ✅ | ✅ |
| 共识度 | ❌ | ✅ |
| 置信区间 | ❌ | ✅ |
| **均值变动曲线**(综合统计均值时间序列) | ❌ | ✅ |
| **修改者身份** | ❌ | ❌ |
| **总评价人数** | ❌ | ❌ |
| **修改时间线**(具体谁改了什么) | ❌ | ❌ |
| **附加信息全文** | ❌ | ❌ |
| **对比功能** | ✅ | ✅ |

> **匿名性是硬底线**:任何账号、任何级别都看不到"谁评的" / "多少人评" / "谁改了什么" / "附加信息内容"。

### 6.4 均值变动曲线

- **数据来源**:
  - 后台定时任务:每小时计算一次"该导师各维度的当前均值"快照
  - 快照存入 `SupervisorAggregateSnapshot` 表(只含时间戳 + 维度 + 均值,不含任何个人数据)
- **展示**:
  - 会员可见,基础账号不可见
  - 时间序列图:X 轴 = 时间(最近 90 天),Y 轴 = 各维度均值(0-100)
  - 可切换维度(单维/全部)
- **粒度**:每小时一个点,90 天 = 2160 个数据点(可下采样到 90 个供展示)
- **不展示**:
  - 具体修改者
  - 具体修改时间(只到小时粒度)
  - 修改时的"理由"或附加信息
  - 任何能反推个人的信息

> **本质区别**:
> - ❌ "修改时间线" = "X 用户在 Y 时间把 Z 维度从 60 改到 75"(个人级)
> - ✅ "均值变动曲线" = "该导师 Z 维度在 Y 小时的均值为 75"(聚合级)

---

## 7. 反滥用与隐私制度

### 7.1 老师账号:软移除
- **检测方式**:
  - 注册时填写的"学校"+"学科"+ 行为模式 + 用户举报
  - 行为指纹:同 IP 多账号 + 集中给特定导师高分
- **处理**:静默丢弃其所有评分(不计入综合分),**不主动通知**
- **反检测**:被软移除的账号仍可浏览,只是评分"无影响"

### 7.2 账号注册要求
- 邮箱必填(不公开)
- **必填学生身份**:学科 + 学校(用于学科相关性加权)
- 内部 ID = `hash(email + school + salt)`
- 选填年级(用于趋势分析)

### 7.3 邮箱可换
- 换邮箱 = 新账号
- 若新账号**同注册来源**被系统识别(同 IP + 同学校 + 同行为模式) → 旧账号数据销毁
- 走自动检测,不需用户申请

### 7.4 账号注销
- 可注销,数据**匿名化保留**(评分仍计入,身份消失)
- 纯删除模式不可选(破坏评分连续性)

### 7.5 多设备登录
- 允许多设备,信誉/评分继承
- 同 IP 限 1 个活跃会话

### 7.6 开放注册 + 速率限制
- 无邀请码
- 基础账号:每天 ≤ 10 条评分
- 会员:每天 ≤ 30 条评分

### 7.7 隐私制度
- **评分内容**:匿名,导师不可见具体评分者
- **附加信息**:不公开(仅审核和聚合权重使用,不展示)
- **证据(URL)**:可被引用,不暴露提交者
- **数据脱敏**:任何对外接口/导出,个人身份信息全部 hash
- **GDPR/个保法合规**:支持账号注销 + 匿名化保留

### 7.8 公开性分级
| 内容 | 未登录 | 基础登录 | 会员 |
|------|--------|----------|------|
| 综合分 | ❌ | ✅ | ✅ |
| 雷达图 | ❌ | ✅ | ✅ |
| 均值变动曲线 | ❌ | ❌ | ✅ |
| 共识度 | ❌ | ❌ | ✅ |
| 置信区间 | ❌ | ❌ | ✅ |
| 对比功能 | ❌ | ✅ | ✅ |

### 7.9 敏感信息处理与加密(防止泄露)

> **目标**:**任何单点泄露都不应导致用户身份或评分内容被还原**。

#### 7.9.1 敏感信息分级

| 级别 | 字段 | 存储方式 | 泄露影响 |
|------|------|---------|----------|
| **P0 极度敏感** | 邮箱、手机号(未来) | AES-256-GCM 字段级加密 | 可被钓鱼/骚扰 |
| **P1 高度敏感** | 学校、学科、IP、行为指纹 | HMAC-SHA256(单向,带 salt) | 可推断身份/定位 |
| **P2 准敏感** | 评分内容、附加信息 | AES-256-GCM 字段级加密 | 可画像评分者 |
| **P3 公开数据** | 综合分、雷达图、共识度 | 明文(本就是公开) | 无 |

#### 7.9.2 静态加密 (At Rest)
- **数据库**:PostgreSQL TDE + 字段级加密
- **加密算法**:AES-256-GCM(认证加密,防篡改)
- **哈希字段**:学校/学科/IP/行为指纹 → HMAC-SHA256 + 站点级 salt
- **密码**:Argon2id(不可还原)
- **备份**:与主库同等加密,密钥分离

#### 7.9.3 传输加密 (In Transit)
- **HTTPS 强制**:全站 TLS 1.3
- **HSTS**:`max-age=31536000; includeSubDomains; preload`

#### 7.9.4 密钥管理 (KMS)
- **开发环境**:本地 KeyStore(.env 加密)
- **生产环境**:云 KMS(阿里云/AWS)— 应用服务器**拿不到主密钥**
- **密钥轮换**:每 **90 天**自动轮换
- **最小权限**:每个服务只拿自己需要的密钥

#### 7.9.5 应用层防护
- **会话**:JWT(15 分钟过期) + Refresh Token(7 天)
- **CSRF**:SameSite=Strict + CSRF Token
- **XSS**:严格 CSP,输出编码
- **暴力破解**:登录速率限制(每 IP 5 次/分钟)
- **异常访问告警**:短时间大量解密/查询请求触发告警

#### 7.9.6 数据脱敏规则
- **导出/备份**:身份字段全部 hash
- **日志**:P0 字段脱敏(`xxx@xxx.com`)
- **测试环境**:必须用脱敏数据
- **错误信息**:不暴露加密字段、堆栈到客户端

#### 7.9.7 数据泄露应急响应
- **检测层**:异常解密请求 + 数据库异常查询 + 暴力破解
- **响应流程**(24h 内):自动告警 → 临时冻结密钥 → 评估范围 → 紧急轮换 → 通知用户(72h 内)
- **演练**:每季度模拟
- **外部披露**:严重泄露(>1000 账号)依法 72h 报告监管

### 7.10 导师匿名系统(无关化名 + k-匿名)

> **设计核心**:
> - 任何用户可创建导师,**无任何限制**,**随意命名**(**不一定是真名**)
> - 审核员**只审核隐私/恶意内容**,**不审核真实性**,**没有查询权力**
> - 后台映射表特征:**任何化名都不得对应真实存在的人名,必须是无关名称**
> - 平台**主动声明**:"化名与真实人名无关,所有内容均为用户虚构,平台不对应任何具体真人"

#### 7.10.1 三层架构

```
┌─────────────────────────────────────────────────┐
│ 公开层(所有用户可见)                              │
│  - 化名(由平台生成,与真实人名**无关**):          │
│  - 学科 / 学院                                    │
│  - 综合分 + 雷达图 + (会员)均值变动曲线            │
│  - 同"学科+学院"分类下 < 10 导师 → 整组不显示     │
└─────────────────────────────────────────────────┘
                    │ k-匿名 + 无关注联
                    ▼
┌─────────────────────────────────────────────────┐
│ 后台层(仅系统,审核员无查询权)                     │
│  - SupervisorNameMapping:                         │
│    * 用户原始名(任意)→ 无关化名                    │
│    * 化名前缀必须与人名白名单无关                  │
│  - 加密:AES-256-GCM,物理隔离                      │
│  - 任何导出/截图/API 都禁止暴露                   │
└─────────────────────────────────────────────────┘
                    │ 物理隔离
                    ▼
┌─────────────────────────────────────────────────┐
│ 数据收集层(用户自由创建)                          │
│  - 名称(任意,真名/化名/乱码均可)                 │
│  - 学科(必填)                                     │
│  - 学院(必填)                                     │
│  - 提交时立即进入 pending_review                   │
│  - 审核:仅检查隐私/恶意内容                        │
└─────────────────────────────────────────────────┘
```

#### 7.10.2 关键规则

| 规则 | 说明 |
|------|------|
| **公开信息 = 化名 + 学科 + 学院** | **仅这三项**,任何其他字段都不展示 |
| **k-匿名 ≥ 10** | 同"学科+学院"组合下,活跃导师数 < 10 → **整组不显示** |
| **映射不暴露** | 任何用户、任何 API、任何导出,**都拿不到**原始名 |
| **化名严格 1-to-1** | 每个化名**只对应一个** Supervisor 档案,跨学科+学院**不可重用** |
| **平台零映射通道** | 不向用户提供任何"原始名 ↔ 化名"查询通道 |
| **公开数据删除** | 任何用户**看不到**"已删除"或"已下架"导师的任何数据 |

#### 7.10.3 化名生成规则(强制)

**化名必须满足**:
1. **与任何真实人名无关**(白名单机制:常见人名表 10000+ 词)
2. **多风格 + 学科融合**(根据"学科+学院"动态选词库):
   - 古风:南飞雁、青松先生
   - 自然:远山、溪流、星辰
   - 几何代号:`Q-7a2b9c`、`AURORA-7K2`
   - **学科融合**:
     - 计算机/AI:α-net、Σ-7b2、λ-qubit
     - 文学/历史:诗酒-远山、青简-墨客
     - 理学/数学:Δ-3a1、∫-curve、ℝ-7k2
     - 医学/生物:cell-Δ、Helix-9k
     - 工程:Ω-relay、vector-7a
     - 经济/管理:σ-market、μ-curve
3. **多字符集**:拉丁 + 希腊(α β γ δ ε ζ η θ λ μ π σ τ φ χ ψ ω)+ 数字 + 数学符号(Δ Σ ∫ π Ω ℝ)
4. **平台统一生成**,审核员**不能**自己取名
5. **不可预测**:不暴露"按什么规则生成"

#### 7.10.4 创建/评价流程

**A. 用户创建导师**:
1. 提交:任意名称 + 学科 + 学院
2. 后端去重:用"用户原始名 hash + 学科 + 学院"查映射表
3. **已存在** → 直接返回该化名
4. **不存在** → 进入 `pending_review`
5. 审核员检查:**仅**隐私/恶意内容
   - 审核员**不查**映射表
   - 审核员**不判断**"是否真名"
6. 通过 → 平台生成**学科融合**的无关化名 → 写入映射 → 公开
7. k-匿名检查:同"学科+学院"分类 < 10 → 整组 hidden

**B. 同一原始名 + 不同学科 = 多个独立档案**:
- 例子:"张伟 + 计算机学院"和"张伟 + 历史学院" → 两个独立档案
- 后端按"原始名 + 学科 + 学院"hash 区分
- 化名**严格 1-to-1**:每个档案的化名**跨学科+学院唯一**

**C. 用户评价**:
- 场景 A:按"学科+学院"搜索 → 看到化名列表 → 点击
- 场景 B:通过化名直接访问(分享链接)
- 场景 C:找不到 → 走"创建导师"流程

**D. 同账号对同化名重复评价**:
1. 用户提交评分时,后端先查"该账号对该化名是否已有评分"
2. **已有评分**:
   - 弹"已评价过"提示(中英)
   - **数据无害化处理**:该账号原始评分仅用于聚合,公开页不显示该账号轨迹
3. **无评分**:正常走"提交评分"流程
4. **修改入口**:用户个人页"修改评价"按钮(走评分覆盖 + 脱敏展示)

#### 7.10.5 法律风险分析(🟢 极低)

| 法规 | 风险等级 | 说明 |
|------|----------|------|
| 民法典 1024(名誉权) | 🟢 极低 | 化名与真实人名**完全无关**,k-匿名 ≥ 10,几乎不可能识别具体真人 |
| 民法典 1025(平台责任) | 🟢 极低 | 平台主动声明"化名无关",**不提供"侵权投诉"功能** |
| 刑法 246(诽谤) | 🟢 极低 | 平台无诽谤意图;化名无关 → 用户评价**不直接指向真人** |
| 民法典 1036(免责) | 🟢 极低 | 免责声明 + 用户协议 + 平台主动声明 + 化名无关性 |

**为什么风险降到极低**:
- 化名与真实人名**完全无关**(白名单强制保证)
- 平台**主动声明**"不对应真人"
- **k-匿名 ≥ 10** 强制保证
- **用户原始名仅作后台记录**,不展示,不查询
- **不提供"侵权投诉"功能** → 平台**不承认"对应真人"概念** → 进一步降低连带责任
- **不做"导师自证"功能** → 强化"不对应真人"立场

**仍需注意**:
- ⚠ **用户原始名表**虽然"用户可能写假名",但也可能"用户写真名" — 后端**仍需加密**(G-8)
- ⚠ **审核员无查询权**:降低平台"明知"的举证风险
- ⚠ **化名生成器需保证无关性**:人名白名单 + 风格化 + 校验
- ⚠ **举报流程仍需**:评价内容可能违规,需要举报 → 审核机制

#### 7.10.6 平台主动声明(强制)

站内每个化名条目下显示:
> ⚠ **平台声明**:本条目"化名"为平台生成,与任何真实人名**无对应关系**。
> 所有评分内容均为用户**虚构提交**,平台不对应任何具体真人。
> 平台不提供"侵权投诉"功能 — 因为没有任何指向真人的实名信息。

#### 7.10.7 实施前必须完成

- ⚠ **法律咨询**:律师评估"无关化名 + 用户自由命名"在民法典下的合规性
- ⚠ **免责声明 + 用户协议 + 隐私政策**(律师审核)
- ⚠ **化名生成器**:词库 10000+ + 人名白名单 + 学科符号库
- ⚠ **k-匿名机制**:DB + API 双层校验
- ⚠ **审核员界面**:只看用户提交内容,看不到映射表
- ⚠ **强制声明弹窗**:首次访问 + 提交评分前 + 创建导师前
- ⚠ **举报 → 审核流程**:任何用户可举报,后台审核处理

---

## 8. 数据模型

```
Account
  ├── id (hash)
  ├── email_enc (P0, AES-256-GCM)
  ├── email_hash (HMAC-SHA256,用于查找)
  ├── student_profile
  │   ├── discipline_hash (P1, HMAC-SHA256)
  │   ├── institution_hash (P1, HMAC-SHA256)
  │   └── grade_enc (P2, AES-256-GCM)
  ├── password_hash (Argon2id)
  ├── tier (basic | member)
  ├── joined_at
  ├── last_active_at
  └── soft_removed (bool, default false)

BehaviorFingerprint
  ├── id
  ├── account_id → Account
  ├── ip_hash (P1, HMAC-SHA256)
  ├── user_agent_hash
  ├── device_fingerprint_hash
  └── recorded_at

Supervisor  (公开视图)
  ├── id
  ├── public_code (化名,如 "α-net-7k2",所有用户可见)
  ├── discipline (学科,公开)
  ├── college (学院,公开)
  ├── composite_score (公开)
  ├── radar_dimensions (公开)
  ├── review_status (pending_review|approved|rejected|hidden)
  ├── k_anonymity_count (同"学科+学院"组合下当前活跃导师数,< 10 则整组 hidden)
  ├── created_at
  └── merged_into (可选,处理重复)

SupervisorNameMapping  (后台映射,⚠ 物理隔离 P0 加密)
  ├── id
  ├── supervisor_id → Supervisor
  ├── submitted_name_enc (P0, AES-256-GCM,用户提交名称加密)
  ├── submitted_name_hash (HMAC-SHA256,用于"原始名+学科+学院"去重)
  ├── discipline (用于去重)
  ├── college (用于去重)
  ├── generated_alias (化名,必须与人名白名单无关)
  ├── alias_generation_seed (化名生成种子,用于复现)
  ├── created_by (创建者 account_id,审计)
  ├── created_at
  └── updated_at

AliasGenerator  (化名生成器,⚠)
  ├── 词库(多风格 × 学科融合):古风 + 自然 + 几何 + 学科符号库
  ├── 字符集(多字母 + 数字):拉丁 + 希腊(α-ω)+ 数字 + 数学符号(Δ Σ ∫ π Ω ℝ)
  ├── 人名白名单:中国常见姓氏 100+ + 常见名字 5000+ + 海外常见人名,每季度更新
  ├── 生成流程:随机选词 + 学科符号 + 哈希后缀 + 白名单校验 + 唯一性校验
  └── 维护:每季度补充新词 + 更新白名单 + 扩展学科符号库

SupervisorCreationRequest  (新建导师请求)
  ├── id
  ├── submitter_id → Account
  ├── submitted_name (明文,审核员可见,只审隐私/恶意)
  ├── discipline (明文,用于映射)
  ├── college (明文,用于映射)
  ├── review_status (pending_review|approved|rejected)
  ├── reviewer_id
  ├── review_notes (审核员备注,只关于"是否含隐私/恶意")
  ├── created_at
  ├── resolved_at
  └── resolved_supervisor_id (审核通过后 → Supervisor.id)

Rating
  ├── id
  ├── account_id → Account
  ├── supervisor_id → Supervisor
  ├── dim
  ├── value (0–100,默认非负)
  ├── dim_additional_enc (P2, 该维度附加信息,AES-256-GCM,仅审核用)
  ├── overall_additional_enc (P2, 整体附加信息,AES-256-GCM,仅审核用)
  ├── additional_level (L1–L4, NLP 自动识别)
  ├── evidence (array of URL,匿名)
  ├── discipline_hash (P1, snapshot)
  ├── created_at
  ├── superseded_by (修改链,指向新 Rating)
  ├── review_status (pending_review|approved|rejected,默认 pending_review)
  ├── review_started_at
  ├── review_completed_at
  ├── reviewer_id
  ├── review_notes
  ├── sensitivity_flags (P0 严禁/P1 脱敏/P2 警告 自动检测结果)
  └── redacted_additional_enc (脱敏后版本,审核通过时用这个)

SupervisorAggregateSnapshot  (均值变动曲线数据源)
  ├── id
  ├── supervisor_id → Supervisor
  ├── snapshot_time (每小时一次)
  ├── composite_mean (综合分均值,0-100)
  ├── dim_means (6 维均值 JSON)
  ├── consensus (共识度 0-1)
  ├── confidence (置信区间 JSON)
  └── created_at

DisciplineWeightVote
  ├── id
  ├── discipline_hash
  ├── dim
  ├── proposed_weight
  ├── proposer_id (private)
  ├── agree_count
  ├── disagree_count
  ├── status (pending|applied|rejected)
  └── applied_at

Evidence
  ├── id
  ├── subject_supervisor_id
  ├── comparator_supervisor_id (可选,用于相对修正)
  ├── dim
  ├── payload (URL/text)
  ├── submitter_id (private)
  ├── status (pending|applied|contested|rejected)
  └── applied_at

AccessLog
  ├── id
  ├── account_id
  ├── resource (e.g., supervisor detail)
  ├── tier_at_access
  ├── ip_hash (P1)
  └── timestamp

Report  (举报,v0.9 新增)
  ├── id
  ├── reporter_id → Account
  ├── target_type (rating | supervisor | additional_info)
  ├── target_id
  ├── reason (诽谤/侮辱/隐私/科研泄露/其他)
  ├── description
  ├── status (pending|reviewing|resolved|dismissed)
  ├── reviewer_id
  ├── resolution (下架/警告/拒绝/不处理)
  ├── submitted_at
  └── resolved_at

EncryptionAuditLog
  ├── id
  ├── field_accessed (e.g., "Account.email_enc")
  ├── account_id
  ├── accessor (service name)
  ├── purpose (e.g., "login", "export")
  └── timestamp
```

> **字段命名约定**:
> - `*_enc` = AES-256-GCM 加密(可还原)
> - `*_hash` = HMAC-SHA256 单向(只可匹配,不可还原)
> - `password_hash` = Argon2id(密码专用)
> - 裸字段 = 明文(必须是公开数据)

---

## 9. UI / UX 草案

### 9.1 主页
- **搜索框**(按"学科+学院"组合)
- **热门导师榜**(按综合分 + 热度)
- **新晋导师**(7 天内新发布)
- **最新动态**(匿名评分修改流,不显示身份)
- **学科入口**(点击进入学科,看学科自适应权重)

### 9.2 导师详情页
```
[化名:α-net-7k2] [学科:计算机] [学院:信息学院]  [🔒 综合分需登录]

─────────────────────────────────────
[登录后可见]

综合推荐分: 75.3 / 100  (🔒 共识度/置信区间/均值曲线需会员)

维度画像:
  科研能力 ████████░░ 78
  资源调度 ██████░░░░ 62
  学科适配 ███████░░░ 70
  领域跟进 ████████░░ 80
  行为正当 █████░░░░░ 54
  工具支持 █████████░ 90

[会员专享]
  均值变动曲线: [时间序列图]
  共识度: 0.82
  置信区间: ±5.2

[平台声明:化名与真人无关]

─────────────────────────────────────
[我要评分] [对比] [举报] [升级会员]
```

### 9.3 提交评分页
```
[化名:α-net-7k2]  [已评价过?修改评价]

科研能力 [====●=====] 75  [+ 添加附加信息]
资源调度 [======●===] 65  [+ 添加附加信息]
学科适配 [=====●====] 50  [+ 添加附加信息]
领域跟进 [======●===] 65  [+ 添加附加信息]
行为正当 [====●=====] 75  [+ 添加附加信息]
工具支持 [======●===] 65  [+ 添加附加信息]

总附加信息(对整体评价,可选):
[_________________________]

[提交评分]
```

### 9.4 对比页(任何用户可用)
- 选 2-5 个化名 → 双/多雷达图
- 综合分并列展示
- 不显示附加信息内容

### 9.5 学科权重投票页
- 当前权重展示(雷达图)
- 投票按钮:"我建议调整"
- 投票历史(匿名)

### 9.6 升级会员页
- 会员权益清单(均值变动曲线/共识度/置信区间)
- 付费入口
- 不展示广告 / 不推送

### 9.7 评价后感谢弹窗
- 触发时机:提交评分后立即弹出
- 文案(中英):
  - 中文:"感谢你的贡献!你的评分已提交审核,通过后将纳入数据。"
  - 英文:"Thanks for contributing! Your rating has been submitted for review and will appear once approved."
- 信息:当前评分状态(`pending_review`) + 预计审核时间
- 不展示其他评分者的评分(避免锚定)

### 9.8 强制声明弹窗
- 首次访问:平台声明 + 化名无关 + 不提供侵权投诉
- 创建导师前:声明条目可能为虚构
- 提交评分前:声明评分进入公开数据
- 用户协议 + 隐私政策勾选

---

## 10. 技术选型

### 10.1 已确定(v1.0)

| 项 | 选型 | 备注 |
|----|------|------|
| **后端** | **Rust** | 用户确定 |
| **数据库** | **PostgreSQL** | 用户确定 |
| **搜索引擎** | **Postgres FTS** | 用户确定(初期够用) |
| **前端** | **React** | 用户确定 |

### 10.2 仍待讨论

| 项 | 候选 | 备注 |
|----|------|------|
| Rust Web 框架 | actix-web / axum / rocket | 看性能/异步需求 |
| Rust ORM | sqlx / diesel / sea-orm | 看迁移/类型推导偏好 |
| Rust 加密库 | `aes-gcm` crate / `ring` / `libsodium-sys` | 见 G-8 |
| 前端状态 | Redux Toolkit / Zustand / Jotai | 看复杂度 |
| 前端构建 | Vite / Next.js | 看 SSR 需求(可能不需要) |
| 国际化 | react-i18next / react-intl | 中英双语 |
| 部署 | Vercel / 自托管(Docker) / Cloudflare | 看规模 + 成本 |
| 支付 | Stripe / 支付宝 / 微信 | 会员付费(海外/国内) |
| **KMS** | 阿里云 KMS / AWS KMS / HashiCorp Vault | 见 G-9 |
| **审计日志** | 自建 + Sentry / Datadog | 异常访问告警 |
| **依赖扫描** | `cargo audit` + npm audit | 每周扫描 |

### 10.3 Rust + PostgreSQL 技术栈要点

- **Rust Web**:actix-web 或 axum(异步 + 高并发)
- **Postgres 客户端**:`sqlx`(编译时 SQL 校验,无运行时反射)或 `diesel`(类型安全 ORM)
- **加密 crate**:
  - AES-256-GCM:`aes-gcm` 或 `ring`
  - HMAC-SHA256:`hmac` + `sha2`
  - Argon2id:`argon2`
  - TLS:`rustls`
- **数据库迁移**:`sqlx migrate` 或 `diesel migrations`
- **监控**:`tracing` + `tracing-subscriber`(结构化日志)
- **错误处理**:`thiserror`(库)+ `anyhow`(应用)

> **关键**:Rust 类型系统 + 编译时 SQL 校验(`sqlx`)能提前捕获大量错误,适合金融/隐私敏感系统。

---

## 11. 里程碑

- **M0 — 文档 + 数据模型确认**(本轮)
  - ✅ 大纲定稿 v0.9
  - ✅ 敏感信息处理与加密制度(§7.9)
  - ✅ 导师匿名系统无关化名 + k-匿名(§7.10)
  - ⏳ 数据模型细化(已标注加密字段)
- **M1 — MVP 后端(Rust + PostgreSQL)**
  - **基础设施**:
    - Rust 项目脚手架(`cargo init` + 依赖:`actix-web`/`axum`, `sqlx`, `aes-gcm`, `argon2`, `redis`)
    - PostgreSQL 数据库初始化(迁移脚本)
    - Redis 缓存层
    - Docker Compose 本地开发环境
  - **账号注册**(G-8 加密):
    - 邮箱必填 + 学科 + 学校
    - AES-256-GCM 加密邮箱(本地 KeyStore,开发期)
    - HMAC-SHA256 单向哈希(用于查询)
    - Argon2id 密码哈希
  - **导师创建请求**:
    - 任意名 + 学科 + 学院
    - HMAC-SHA256 hash 去重
    - 进入 `pending_review`
  - **化名生成器 v0.8**:
    - 词库(古风/自然/几何/学科融合)
    - 多字符集(拉丁 + 希腊 + 数字 + 数学符号)
    - 人名白名单 10000+ 词
    - 严格 1-to-1 唯一
  - **评分 CRUD**:
    - 滑块 0-100 + 附加信息(可选)
    - 走 `pending_review` 流程
  - **聚合 API**:综合分 + 雷达图(基础账号可见)
  - **登录墙 + 会员墙**:JWT(15 分钟) + Refresh Token(7 天)
- **M2 — 学科自适应权重**
  - 投票机制
  - 权重动态计算
- **M3 — 反滥用 + 隐私**
  - 老师账号软移除
  - 邮箱换绑销毁
  - 数据脱敏导出
  - **举报 → 审核流程**
- **M4 — 前端**
  - 搜索/列表/详情
  - 提交评分(含负分受控)
  - 化名校验 + 强制声明弹窗
  - 会员升级
  - 对比功能(任何用户)
- **M5 — 国际化 + 公开测试**
  - i18n 完整覆盖
  - 种子数据
  - 邀请试用
  - 调参迭代
- **M6 — 安全加固(独立里程碑,贯穿 M1–M5)**
  - 字段级加密落地
  - KMS 集成 + 密钥轮换机制
  - HTTPS + HSTS 强制
  - 加密审计日志
  - 异常访问告警
  - 季度泄露演练
  - 依赖 CVE 扫描自动化
- **M7 — 审核 + 极简公开匿名(无关化名版,法律咨询前不可启动)**
  - ⚠ **前置条件**:律师法律意见书(民法典 1024/1025/1036,刑法 246)+ 化名无关性方案评估
  - 人工审核系统(审核员界面 — **只看提交内容,无查询权**)
  - 敏感信息检测(NLP 关键词 + 模式识别)
  - **极简公开匿名系统**:
    - 公开字段:**化名(平台生成,与真人无关)** + 学科 + 学院 + 综合分 + 雷达图
    - k-匿名 ≥ 10 保护(< 10 整组不显示)
    - **化名生成器**:多风格 + 学科融合 + 多字符集 + 严格 1-to-1
    - 后台原始名映射表(AES-256-GCM,物理隔离)
  - 用户自由创建流程
  - 同一原始名不同学科 → **多个独立档案**
  - 平台主动声明(每个化名条目下)
  - **不做**:侵权投诉 / 误认救济 / 导师自证 / 导出
  - **做**:举报 → 审核流程
  - **重复评价提示**(G-21):同账号已评过 → 弹"已评价"提示 + 数据无害化
  - **均值变动曲线**(G-24):会员可见,聚合级时间序列
  - 强制声明弹窗(首次访问 + 提交评分前 + 创建导师前)
  - 免责声明 + 用户协议 + 隐私政策(经律师审核)

---

## 12. 待讨论(详见 DECISIONS.md)

完整决策清单见 **[`DECISIONS.md`](./DECISIONS.md)**。

### 12.1 已确认的核心决策
- A-1 ☑ 工具 + 软评论(避免过激讨论)
- A-2 ☑ 用户分层(消费端/数据源/老师禁区)
- A-3 ☑ 双层墙(登录墙 + 会员墙)
- A-4 ☑ 部分免费 + 详情收费
- A-5 ☑ 中英双语
- B-1–B-9 ☑ 身份与限制
- C-1 ☑ 0–100 分制
- C-2 ☑ 学科自适应权重 + 用户共同投票
- C-3 ☑ 分级显示
- C-4 ☑ 维度可扩展,按学科调整
- C-5 ☑ 允许负分(受控开放)
- C-6 ☑ 时间衰减 0.1
- C-7 ☒ 不需要账号信誉
- C-8 ☑ NLP 自动分级
- C-9 ☒ 不做匹配度
- C-10 ☑ 评分表单(滑块+附加信息)
- C-11 ☑ 均值变动曲线
- D-? ❓ 动态相对修正(延后)
- E-1 ☑ 老师账号软移除
- E-2 ☑ 邮箱 + CAPTCHA
- E-3 ☑ 评分速率限制
- E-4 ☑ 同 IP 限制
- E-5 ⚠ 仲裁流程
- E-6 ☑ 申诉流程
- E-7 ☑ 黑名单公开
- E-8 ☑ 评分前不显示群体信息
- F-1 ☑ 主页信息密度(中等)
- F-2 ❓ 搜索维度
- F-3 ☑ 登录墙策略
- F-4 ☑ 移动端 + 桌面端
- F-5 ❓ 评分表单流程
- F-6 ❓ 修改历史 UI
- F-7 ☑ 对比功能(任何用户)
- F-8 ☒ 不做导出
- F-9 ☒ 不做通知
- F-10 ☒ 不做讨论区
- F-11 ☑ 评价后感谢弹窗
- G-1 ☑ 隐私合规
- G-2 ☑ 敏感词 + 用户举报
- G-3 ☑ 举报功能
- G-4 ☒ 不做导师自证
- G-5 ☒ 不做广告
- G-6 ☑ 跨学科可比性
- G-7 ☑ 评判项目隐私制度
- G-8 ☑ 敏感信息分级 + 加密
- G-9 ❓ KMS 选型
- G-10 ❓ 泄露应急响应
- G-11 ☑ 人工审核系统
- G-12 ☑ 敏感信息禁止(科研内容)
- G-13 ☑ 导师匿名系统(无关化名)
- G-14 ☑ k-匿名 ≥ 10
- G-15 ☑ 后台原始名映射
- G-16 ☑ 导师创建流程
- G-17 ☑ 化名生成器(多风格+学科融合)
- G-18 ☑ 评价入口
- G-19 ☑ 同名条目去重
- G-20 ☑ 化名严格 1-to-1
- G-21 ☑ 重复评价提示
- G-22 ☑ 评价修改
- G-23 ☑ 评分表单
- G-24 ☑ 均值变动曲线
- G-25 ☑ 数据保留期永久
- G-26 ☑ 评分公式可后续修改
- G-27 ☒ 不做侵权投诉
- G-28 ☒ 不做导师自证
- H-1 ☑ 后端 = Rust
- H-2 ☑ 数据库 = PostgreSQL
- H-3 ❓ 存储(证据附件)
- H-4 ☑ 搜索引擎 = Postgres FTS
- H-5 ☑ 前端 = React
- H-6 ❓ 部署
- H-7 ❓ 监控
- H-8 ❓ 邮件服务
- H-9 ☑ CI/CD = GitHub Actions
- H-10 ❓ 支付渠道
- I-1 ❓ 冷启动
- I-2 ❓ 推广
- I-3 ☑ 不做"机构"层
- I-4 ☒ 不做学术合作
- I-5 ☑ 国际化 = 中英双语
- I-6 ❓ 公开 API
- J-1 ☑ 永久
- J-2 ☑ 可修改
- J-3 ❓ 修正规则可调整
- J-4 ❓ DB 迁移
- J-5 ❓ 向后兼容

### 12.2 仍需澄清的 11 个
详见 DECISIONS.md "仍需澄清的关键点"。

---

## 13. 一句话回到核心

> **没有任何一个维度、任何一个分数,可以独立告诉用户"这个导师好/坏"。**
> 系统提供的是:**多维画像 + 综合分 + (会员)共识度/置信区间/均值变动曲线**。
> **判断**永远在用户手里。

---

## 14. 设计演变史

| 版本 | 主要变化 | 状态 |
|------|---------|------|
| **v0.1** | 初版:6 维 + 守恒修正 + 信誉公式 | 历史 |
| **v0.2** | 重构:工具+软评论定位 / 用户分层 / 0-100 / 学科自适应权重 / 受控负分 / 评分人数限制 / 付费会员 / 老师软移除 / NLP 理由分级 | 历史 |
| **v0.3** | 加密制度 + 数据脱敏 + KMS 密钥管理 + 泄露响应 | 历史 |
| **v0.4** | 人工审核 + 敏感信息 + 导师匿名(初步) | 历史 |
| **v0.5** | 知情者感知型(平台零映射 + 公开大区/资历/方向/描述) | **已废** |
| **v0.6** | 极简公开 + k-匿名 + 后台真名映射 | **已废** |
| **v0.7** | 推翻"后台存真名"假设 → 无关化名 + 用户自由命名 + 审核员无查询权 | 当前 |
| **v0.8** | 化名生成器升级(多风格+学科融合+多字符集) + 重复评价提示 + 严格 1-to-1 | 当前 |
| **v0.9** | 评分表单重构(滑块+附加信息) + 均值变动曲线(替代修改历史) + 简化(去侵权/自证/导出) | 历史 |
| **v1.0** | 清理废设计残留 + 锁定技术栈(Rust + PostgreSQL + Postgres FTS + React) + 不做学术合作 | **当前** |

**关键设计决策**:
- v0.5 → v0.6 转变:从"零映射" → "真名映射"是设计反复(用户最初想完全不存真名,后改主意)
- v0.6 → v0.7 转变:再次推翻"真名映射" → 改为"无关化名"(用户明确化名必须与真名无关)
- v0.8 → v0.9 转变:删除"修改时间线"展示,改为"均值变动曲线"(更彻底的隐私)
- v1.0 锁栈:后端 Rust(类型安全 + 高性能,适合隐私敏感系统)+ PostgreSQL(关系型 + JSONB + FTS 一体化)
